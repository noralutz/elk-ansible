output {
  elasticsearch {
    hosts => ["{{ hostvars[groups['elasticsearch-host'][0]]['ansible_fqdn'] }}:9200"]
    sniffing => true
    manage_template => false
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
    document_type => "%{[@metadata][type]}"
  }
}
